services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=typinglab'
      - 'POSTGRES_PASSWORD=1234'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
